<h1>Posts</h1>

<ul>
  <% @posts.each do |post| %>
    <li>
      [<%= link_to 'x', post_path(post), method: :delete, data: { confirm: 'are u sure'  } %>]
      <%= link_to post.title, post_path(post) %>
    </li>
  <% end %>
</ul>

<%= link_to 'new post', new_post_path %>
